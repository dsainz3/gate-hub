# =========================================================
# KNEPLATT72 — 24h STATISTICS
# =========================================================

# --- Averages ---
  - platform: statistics
    name: KNEPLATT72 Wind Speed (24h Avg)
    unique_id: kneplatt72_wind_speed_24h_avg
    entity_id: sensor.kneplatt72_wind_speed
    state_characteristic: mean
    max_age:
      hours: 24
    precision: 1

  - platform: statistics
    name: KNEPLATT72 Temperature (24h Mean)
    unique_id: kneplatt72_temperature_24h_mean
    entity_id: sensor.kneplatt72_temperature
    state_characteristic: mean
    max_age:
      hours: 24
    precision: 1

  - platform: statistics
    name: KNEPLATT72 Humidity (24h Mean)
    unique_id: kneplatt72_humidity_24h_mean
    entity_id: sensor.kneplatt72_relative_humidity
    state_characteristic: mean
    max_age:
      hours: 24
    precision: 1

# --- Changes ---
  - platform: statistics
    name: KNEPLATT72 Pressure (24h Change)
    unique_id: kneplatt72_pressure_24h_change
    entity_id: sensor.kneplatt72_pressure
    state_characteristic: change
    max_age:
      hours: 24
    precision: 3

  - platform: statistics
    name: KNEPLATT72 Temperature (24h Change)
    unique_id: kneplatt72_temperature_24h_change
    entity_id: sensor.kneplatt72_temperature
    state_characteristic: change
    max_age:
      hours: 24
    precision: 1

  - platform: statistics
    name: KNEPLATT72 Humidity (24h Change)
    unique_id: kneplatt72_humidity_24h_change
    entity_id: sensor.kneplatt72_relative_humidity
    state_characteristic: change
    max_age:
      hours: 24
    precision: 1
# =========================================================
# HUMIDOR - STATISTICS & TRENDS
# =========================================================

  - platform: statistics
    name: Humidor Temp Stats (24h)
    unique_id: humidor_temp_stats_24h
    entity_id: sensor.hygrometer_humidor_temperature
    state_characteristic: mean
    max_age:
      hours: 24
    precision: 1

  - platform: statistics
    name: Humidor Temp Change (1h)
    unique_id: humidor_temp_change_1h
    entity_id: sensor.hygrometer_humidor_temperature
    state_characteristic: change
    max_age:
      hours: 1
    precision: 2

  - platform: statistics
    name: Humidor Humidity Stats (24h)
    unique_id: humidor_humidity_stats_24h
    entity_id: sensor.hygrometer_humidor_humidity
    state_characteristic: mean
    max_age:
      hours: 24
    precision: 1

  - platform: statistics
    name: Humidor Humidity Change (1h)
    unique_id: humidor_humidity_change_1h
    entity_id: sensor.hygrometer_humidor_humidity
    state_characteristic: change
    max_age:
      hours: 1
    precision: 2
# Repository TODO tracker
  # Command line sensors moved to configuration.yaml under command_line:
# SPRINKLERS — RUNTIME TRACKING

  - platform: history_stats
    name: irrigation_zone1_hours_today
    entity_id: switch.sprinkler_south_side_zone
    state: "on"
    type: time
    start: "{{ now().replace(hour=0, minute=0, second=0, microsecond=0) }}"
    end: "{{ now() }}"

  - platform: history_stats
    name: irrigation_zone1_hours_month
    entity_id: switch.sprinkler_south_side_zone
    state: "on"
    type: time
    start: "{{ now().replace(day=1, hour=0, minute=0, second=0, microsecond=0) }}"
    end: "{{ now() }}"

  - platform: history_stats
    name: irrigation_zone2_hours_today
    entity_id: switch.sprinkler_back_south_zone
    state: "on"
    type: time
    start: "{{ now().replace(hour=0, minute=0, second=0, microsecond=0) }}"
    end: "{{ now() }}"

  - platform: history_stats
    name: irrigation_zone2_hours_month
    entity_id: switch.sprinkler_back_south_zone
    state: "on"
    type: time
    start: "{{ now().replace(day=1, hour=0, minute=0, second=0, microsecond=0) }}"
    end: "{{ now() }}"

  - platform: history_stats
    name: irrigation_zone3_hours_today
    entity_id: switch.sprinkler_back_north_zone
    state: "on"
    type: time
    start: "{{ now().replace(hour=0, minute=0, second=0, microsecond=0) }}"
    end: "{{ now() }}"

  - platform: history_stats
    name: irrigation_zone3_hours_month
    entity_id: switch.sprinkler_back_north_zone
    state: "on"
    type: time
    start: "{{ now().replace(day=1, hour=0, minute=0, second=0, microsecond=0) }}"
    end: "{{ now() }}"

  - platform: history_stats
    name: irrigation_zone4_hours_today
    entity_id: switch.sprinkler_front_zone
    state: "on"
    type: time
    start: "{{ now().replace(hour=0, minute=0, second=0, microsecond=0) }}"
    end: "{{ now() }}"

  - platform: history_stats
    name: irrigation_zone4_hours_month
    entity_id: switch.sprinkler_front_zone
    state: "on"
    type: time
    start: "{{ now().replace(day=1, hour=0, minute=0, second=0, microsecond=0) }}"
    end: "{{ now() }}"

  - platform: history_stats
    name: irrigation_zone5_hours_today
    entity_id: switch.sprinkler_zone_5_zone
    state: "on"
    type: time
    start: "{{ now().replace(hour=0, minute=0, second=0, microsecond=0) }}"
    end: "{{ now() }}"

  - platform: history_stats
    name: irrigation_zone5_hours_month
    entity_id: switch.sprinkler_zone_5_zone
    state: "on"
    type: time
    start: "{{ now().replace(day=1, hour=0, minute=0, second=0, microsecond=0) }}"
    end: "{{ now() }}"
