# Loads default set of integrations. Do not remove.
---
default_config:

# Load frontend themes from the themes folder
frontend:
  themes: !include_dir_merge_named themes

automation: !include_if_exists external/haos/automations.yaml
script: !include_if_exists external/haos/scripts.yaml
scene: !include_if_exists external/haos/scenes.yaml
group: !include_if_exists external/haos/groups.yaml

# Test commit: verifying sync
