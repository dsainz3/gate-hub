# Loads default set of integrations. Do not remove.
---
default_config:

# Load frontend themes from the themes folder
frontend:
  themes: !include_dir_merge_named themes

automation: !include external/haos/automations.yaml
script: !include external/haos/scripts.yaml
scene: !include external/haos/scenes.yaml
group: !include external/haos/groups.yaml

# Test commit: verifying sync
